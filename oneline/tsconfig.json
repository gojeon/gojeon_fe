{
    "compilerOptions": {
        "target": "ESNext", // 최신 JS 기능을 사용할 수 있게 설정
        "module": "ESNext", // 최신 모듈 시스템 사용
        "jsx": "react-jsx", // React에서 JSX 사용 가능하도록 설정
        "jsxImportSource": "@emotion/react",
        "strict": true, // 타입스크립트 엄격 모드 활성화
        "moduleResolution": "node", // Node.js 방식 모듈 해석
        "esModuleInterop": true, // CommonJS 모듈 간 호환성
        "skipLibCheck": true, // 라이브러리의 타입 검사 생략
        "forceConsistentCasingInFileNames": true
    },
    "include": ["src", "src/emotion.d.ts"], // src 디렉토리의 파일들을 포함
    "exclude": ["node_modules"] // node_modules 제외
}
